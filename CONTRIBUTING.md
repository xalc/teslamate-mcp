# Contributing

Run `uv run --extra test pytest -q` and `docker compose config -q` before opening
a pull request. Use fictional locations and coordinates in tests and examples.
Never commit runtime secrets, private hostnames, database exports or real trip
data.
