# Security

Do not open public issues containing vehicle locations, routes, database dumps,
access tokens, receipts or deployment addresses. Report vulnerabilities through
GitHub private vulnerability reporting when it is available for this repository.

The default location mode is `coarse`; it is a privacy aid, not an access-control
boundary. Keep both MCP endpoints authenticated and reachable only through a
trusted private network.
